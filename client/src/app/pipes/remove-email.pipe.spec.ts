import { RemoveEmailPipe } from './remove-email.pipe';

describe('RemoveEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
